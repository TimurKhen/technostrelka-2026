import { ViewportAnimation } from './viewport-animation';

describe('ViewportAnimation', () => {
  it('should create an instance', () => {
    const directive = new ViewportAnimation();
    expect(directive).toBeTruthy();
  });
});
