import { PathConverterPipe } from './path-converter.pipe';

describe('PathConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PathConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
